# Yew component crates directory
Yew UI components should be divided into separate crates for better compilation times.
## Naming convention
A components crate name should be in the following format: `chipbox-ui-{component}`.
