{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "backend",
            "type": "lldb",
            "request": "attach",
            "sourceLanguages": [
                "rust"
            ],
            "program": "${workspaceFolder}/target/debug/chipbox.exe",
        },
        {
            "name": "frontend",
            "type": "msedge",
            "request": "attach",
            "port": 9222,
            "webRoot": "${workspaceFolder}/dist",
        }
    ],
}
